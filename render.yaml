services:
  - type: web
    name: brkl
    env: static
    buildCommand: echo "No build needed - static site"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
